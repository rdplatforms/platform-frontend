import { PlaceholderPage } from './PlaceholderPage';

export function UsersPage() {
  return (
    <PlaceholderPage
      title="Users"
      description="Will manage platform operators and per-business access once authentication and roles exist. See docs/future-admin.md."
    />
  );
}
