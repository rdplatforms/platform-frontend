import { PlaceholderPage } from './PlaceholderPage';

export function DashboardPage() {
  return (
    <PlaceholderPage
      title="Dashboard"
      description="Will summarize business health across the platform once the backend exists: active businesses, recent content changes, and system status."
    />
  );
}
