import { PlaceholderPage } from './PlaceholderPage';

export function PagesPage() {
  return (
    <PlaceholderPage
      title="Pages"
      description="Will let an operator enable/disable and reorder sections per business page — editing the same PageConfig structure the website already renders from."
    />
  );
}
