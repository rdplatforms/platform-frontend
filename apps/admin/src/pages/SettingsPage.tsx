import { PlaceholderPage } from './PlaceholderPage';

export function SettingsPage() {
  return (
    <PlaceholderPage
      title="Settings"
      description="Will expose per-business operational settings — currency, locale, timezone, booking and WhatsApp toggles — backed by BusinessSettings."
    />
  );
}
