import { PlaceholderPage } from './PlaceholderPage';

export function ServicesPage() {
  return (
    <PlaceholderPage
      title="Services"
      description="Will provide CRUD for each business's service catalog — the same ServiceItem records ServiceCatalogService already serves to the website."
    />
  );
}
