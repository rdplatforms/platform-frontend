import { PlaceholderPage } from './PlaceholderPage';

export function ThemePage() {
  return (
    <PlaceholderPage
      title="Theme"
      description="Will let an operator adjust a business's colors, typography, button style, and border radius with a live preview of the theme engine's output."
    />
  );
}
