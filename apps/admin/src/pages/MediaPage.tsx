import { PlaceholderPage } from './PlaceholderPage';

export function MediaPage() {
  return (
    <PlaceholderPage
      title="Media"
      description="Will manage uploaded logos, gallery photos, and service images per business once media storage exists behind the Spring Boot backend."
    />
  );
}
