import { PlaceholderPage } from './PlaceholderPage';

export function BusinessPage() {
  return (
    <PlaceholderPage
      title="Business"
      description="Will manage core business identity — contact info, hours, domains, and social links — the same Business record BusinessResolver matches against."
    />
  );
}
