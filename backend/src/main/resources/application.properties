spring.application.name=backend
server.port=8081

spring.datasource.url=jdbc:postgresql://localhost:5433/rdplatforms
spring.datasource.username=rdplatforms
spring.datasource.password=rdplatforms

# Flyway owns the schema; Hibernate must never auto-generate/alter DDL.
spring.jpa.hibernate.ddl-auto=validate
spring.flyway.enabled=true

# Dev-only signing secret — generated for local development, not a
# production credential. Override app.jwt.secret via an env var
# (SPRING_APPLICATION_JSON, a real secrets manager, etc.) for any
# deployed environment; never commit a real one.
app.jwt.secret=a6db2b8beaeff17d5cf69e6e8cbe034df5356630606f70f33a0000d714b934d9
app.jwt.expiration-minutes=1440
